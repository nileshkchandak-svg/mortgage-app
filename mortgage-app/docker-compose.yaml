version: "3.9"

services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
    ports:
      - "8080:8080"
    networks:
      - mortgage-network

networks:
  mortgage-network:
    driver: bridge
